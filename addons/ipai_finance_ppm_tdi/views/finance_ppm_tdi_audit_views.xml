<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Finance PPM TDI Audit Tree View -->
    <record id="view_finance_ppm_tdi_audit_tree" model="ir.ui.view">
        <field name="name">finance.ppm.tdi.audit.tree</field>
        <field name="model">finance.ppm.tdi.audit</field>
        <field name="arch" type="xml">
            <list string="Finance PPM Import History" decoration-success="state=='done'"
                  decoration-warning="state=='partial'" decoration-danger="state=='failed'">
                <field name="import_date"/>
                <field name="import_type"/>
                <field name="file_name"/>
                <field name="user_id"/>
                <field name="records_created" sum="Total Created"/>
                <field name="records_updated" sum="Total Updated"/>
                <field name="records_failed" sum="Total Failed"/>
                <field name="success_rate" widget="progressbar"/>
                <field name="state" widget="badge" decoration-success="state=='done'"
                       decoration-warning="state=='partial'" decoration-danger="state=='failed'"/>
            </list>
        </field>
    </record>

    <!-- Finance PPM TDI Audit Form View -->
    <record id="view_finance_ppm_tdi_audit_form" model="ir.ui.view">
        <field name="name">finance.ppm.tdi.audit.form</field>
        <field name="model">finance.ppm.tdi.audit</field>
        <field name="arch" type="xml">
            <form string="Finance PPM Import Audit" create="false" edit="false">
                <header>
                    <button name="action_view_errors" string="View Errors" type="object"
                            class="btn-warning" invisible="not has_errors"/>
                    <button name="action_revert_import" string="Revert Import" type="object"
                            class="btn-danger" groups="ipai_finance_ppm_tdi.group_finance_ppm_admin"
                            confirm="Revert this import? This will delete all records created by this import."
                            invisible="records_created == 0"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_details" type="object" class="oe_stat_button" icon="fa-info-circle">
                            <div class="o_stat_info">
                                <span class="o_stat_text">Details</span>
                            </div>
                        </button>
                    </div>

                    <div class="oe_title">
                        <h1>
                            <field name="display_name"/>
                        </h1>
                    </div>

                    <group>
                        <group name="import_info" string="Import Information">
                            <field name="import_type"/>
                            <field name="file_name"/>
                            <field name="import_date"/>
                            <field name="user_id"/>
                        </group>
                        <group name="statistics" string="Statistics">
                            <field name="total_records"/>
                            <field name="records_created"/>
                            <field name="records_updated"/>
                            <field name="records_skipped"/>
                            <field name="records_failed"/>
                            <field name="success_rate" widget="progressbar"/>
                            <field name="has_errors" invisible="1"/>
                        </group>
                    </group>

                    <notebook>
                        <page name="summary" string="Import Summary">
                            <field name="import_summary" widget="text" nolabel="1"/>
                        </page>
                        <page name="errors" string="Error Log" invisible="not error_log">
                            <field name="error_log" widget="text" nolabel="1"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Finance PPM TDI Audit Search View -->
    <record id="view_finance_ppm_tdi_audit_search" model="ir.ui.view">
        <field name="name">finance.ppm.tdi.audit.search</field>
        <field name="model">finance.ppm.tdi.audit</field>
        <field name="arch" type="xml">
            <search string="Finance PPM Import History">
                <field name="import_type"/>
                <field name="file_name"/>
                <field name="user_id"/>

                <filter name="filter_today" string="Today"
                        domain="[('import_date', '&gt;=', context_today().strftime('%Y-%m-%d 00:00:00'))]"/>
                <filter name="filter_this_week" string="This Week"
                        domain="[('import_date', '&gt;=', (context_today() - datetime.timedelta(days=7)).strftime('%Y-%m-%d 00:00:00'))]"/>
                <filter name="filter_this_month" string="This Month"
                        domain="[('import_date', '&gt;=', (context_today().replace(day=1)).strftime('%Y-%m-%d 00:00:00'))]"/>

                <separator/>
                <filter name="filter_team" string="Team Members" domain="[('import_type', '=', 'team')]"/>
                <filter name="filter_tasks" string="Tasks" domain="[('import_type', '=', 'tasks')]"/>
                <filter name="filter_bir" string="BIR Calendar" domain="[('import_type', '=', 'bir')]"/>
                <filter name="filter_logframe" string="LogFrame" domain="[('import_type', '=', 'logframe')]"/>

                <separator/>
                <filter name="filter_done" string="Completed" domain="[('state', '=', 'done')]"/>
                <filter name="filter_partial" string="Partial Success" domain="[('state', '=', 'partial')]"/>
                <filter name="filter_failed" string="Failed" domain="[('state', '=', 'failed')]"/>
                <filter name="filter_errors" string="Has Errors" domain="[('has_errors', '=', True)]"/>

                <group expand="0" string="Group By">
                    <filter name="group_import_type" string="Import Type" context="{'group_by': 'import_type'}"/>
                    <filter name="group_user" string="User" context="{'group_by': 'user_id'}"/>
                    <filter name="group_state" string="State" context="{'group_by': 'state'}"/>
                    <filter name="group_date" string="Import Date" context="{'group_by': 'import_date:month'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action to open audit log list -->
    <record id="action_finance_ppm_tdi_audit" model="ir.actions.act_window">
        <field name="name">Import History</field>
        <field name="res_model">finance.ppm.tdi.audit</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_filter_this_month': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No import history yet
            </p>
            <p>
                Import history will appear here after you import Finance PPM data.
            </p>
        </field>
    </record>

</odoo>

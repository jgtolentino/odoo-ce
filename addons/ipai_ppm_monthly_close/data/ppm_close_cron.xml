<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- ============================================================ -->
        <!-- Cron Job: Create Monthly Close Schedule                     -->
        <!-- Runs on 3rd business day before month-end                   -->
        <!-- ============================================================ -->
        <record id="cron_create_monthly_close" model="ir.cron">
            <field name="name">Create Next Month's Close Schedule</field>
            <field name="model_id" ref="model_ppm_monthly_close"/>
            <field name="state">code</field>
            <field name="code">model.cron_create_monthly_close()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 02:00:00')"/>
            <field name="priority">5</field>
        </record>

        <!-- ============================================================ -->
        <!-- Cron Job: Daily Task Reminders                              -->
        <!-- Sends notifications to owners/reviewers/approvers           -->
        <!-- ============================================================ -->
        <record id="cron_daily_task_reminders" model="ir.cron">
            <field name="name">Send Daily Task Reminders</field>
            <field name="model_id" ref="model_ppm_close_task"/>
            <field name="state">code</field>
            <field name="code">model.cron_send_daily_reminders()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 08:00:00')"/>
            <field name="priority">10</field>
        </record>

    </data>
</odoo>

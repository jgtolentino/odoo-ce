# Production Environment Variables for Odoo CE v0.9.1
# CRITICAL: Never commit this file with real passwords!
# Copy to .env.production and fill in real values

# Database Password
# MUST be strong: 16+ chars, mix of uppercase, lowercase, numbers, symbols
DB_PASSWORD=CHANGE_ME_STRONG_DB_PASSWORD

# Admin Password (Odoo super admin)
# Used to create/manage databases
ADMIN_PASSWORD=CHANGE_ME_SUPERMASTER_PASSWORD

# Optional: DigitalOcean Spaces for backups (if using automated backups)
# DO_SPACES_ACCESS_KEY=
# DO_SPACES_SECRET_KEY=
# DO_SPACES_BUCKET=odoo-backups
# DO_SPACES_REGION=sgp1

# Optional: SMTP for email notifications
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-app-password

# Optional: Monitoring/Alerting
# PROMETHEUS_METRICS_ENABLED=false
# SENTRY_DSN=
